<?php 
$numeros = [1, 2, 3, 4];

/**
 * array_sum() - Devuelve la suma de los valores de un array.
 * 
 * Ejemplo: Sumar los valores de un array numérico.
 */
$suma = array_sum($numeros); // 10

/**
 * array_product() - Devuelve el producto de los valores de un array.
 * 
 * Ejemplo: Calcular el producto de los valores.
 */
$producto = array_product($numeros); // 24


?>
